# Japanese TTS Web 开发与测试指南

本文档记录 Japanese TTS Web 的技术架构、测试用例清单以及后续版本规划。

---

## 1. 系统架构

```text
┌──────────────────────────────────────────────┐
│                  Browser                     │
│  - HTML5 / CSS3 / Vanilla JS                 │
│  - <audio controls> 原生音频播放              │
│  - Blob & URL.createObjectURL 生命周期管理   │
└──────────────────────┬───────────────────────┘
                       │
                       │ HTTP POST /api/tts (JSON)
                       ↓
┌──────────────────────────────────────────────┐
│                 FastAPI                      │
│  - app/main.py: 静态资源挂载 & CORS          │
│  - app/api/tts.py: 校验、异常处理、脱敏日志   │
│  - app/schemas/tts.py: Pydantic 校验 (1~1000)│
└──────────────────────┬───────────────────────┘
                       │
                       │ synthesize(text)
                       ↓
┌──────────────────────────────────────────────┐
│            app/services/tts_service.py       │
│  - google-genai SDK 异步调用                 │
│  - Interactions API (speech_config)          │
│  - PCM 24kHz -> WAV 封装                     │
│  - 统一异常转换 (TTSConfigError/Timeout/等)  │
└──────────────────────┬───────────────────────┘
                       │
                       │ gRPC / HTTPS API
                       ↓
┌──────────────────────────────────────────────┐
│          Gemini TTS Service                  │
│  - Model: gemini-2.5-flash-preview-tts       │
│  - Voice: Kore (30种预置音色可选)            │
│  - Format: 24kHz 16-bit PCM -> WAV           │
└──────────────────────────────────────────────┘
```

---

## 2. 核心测试用例清单

### 2.1 功能与边界测试

| 序号 | 测试场景 | 输入样例 | 预期结果 |
| :--- | :--- | :--- | :--- |
| 1 | 正常陈述句 | `今日はいい天気ですね。` | 成功生成自然语音并自动播放 |
| 2 | 长句连贯性 | `今日は東京に行って、そのあと友達とご飯を食べてから、新宿で買い物をする予定です。` | 语调自然，无中途截断 |
| 3 | 多行文本与换行 | `こんにちは。\n\n今日はいい天気ですね。\n\n明日は東京へ行きます。` | 段落之间有合理的停顿间隔 |
| 4 | 疑问句与感叹句 | `え、本当に？すごい！そうですね……。` | 语调有清晰的起伏升降 |
| 5 | 数字与日期朗读 | `2026年8月19日です。` | 正确按日语习惯朗读日期与数字 |
| 6 | 英文与外来语混杂 | `今日はPythonを勉強します。` | 英文字母或外来语正确发音 |
| 7 | 空输入拦截 | ` ` (纯空格) | 前端和后端均拒绝提交，提示输入内容 |
| 8 | 超长输入拦截 | 超过 1000 字符文本 | 前端红色警示，后端返回 422 拒绝 |

### 2.2 异常与容错测试

| 场景 | 模拟方式 | 预期表现 |
| :--- | :--- | :--- |
| 未配置 API Key | 清空 `.env` 中的 `GEMINI_API_KEY` | 后端返回 400，前端提示配置异常，不暴露密钥细节 |
| 上游服务网络超时 | 设置极短超时时间模拟网络问题 | 捕获超时异常，返回友好重试提示 |
| 非法 JSON 请求 | 发送畸形请求体 | 后端返回 422 校验错误 |

---

## 3. 后续演进路线

- **v0.2 计划**：
  - 支持多声音切换（从 Gemini TTS 30 种音色如 `Kore`, `Puck`, `Zephyr`, `Aoede`, `Leda` 等快速切换）。
  - 支持语速与提示词情感控制（利用 Gemini TTS 的 Director's Chair 表达控制能力）。
  - 支持历史记录缓存列表（Local Storage）。
- **v0.3 计划**：
  - 音频合成服务端缓存（基于 `SHA256(text + voice + model)` 计算 Hash）。
  - 导出/下载 WAV/MP3 音频文件功能。

稳定版的实际部署约束、环境变量、测试和回滚流程以 [STABLE_RELEASE.md](docs/STABLE_RELEASE.md) 为准。
